void main() {
  int n = 10;
  int n2 = 2;
  bool r = n <= n2 && n >= 10;

  // ==
  // !=
  // >
  // <
  // >=
  // <=
  print(n == 15);
  print(r);

  //? Operadores logicos

  //* && (and)
  //* || (or)
  //* ! (negação) Inverte o resultado para o contratorio

  bool r2 = 10 <= 20 && 10 <= 5;

  print(r2);
}
